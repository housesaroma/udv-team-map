# 🗺️ UDV Team Map

Современное веб-приложение для визуализации организационной структуры компании UDV Group. Проект предоставляет интуитивно понятный интерфейс для просмотра иерархии сотрудников, поиска информации о коллегах и управления профилями.

## 🚀 О проекте

UDV Team Map решает ключевые бизнес-задачи:

- **Повышение прозрачности** организационной структуры компании
- **Упрощение поиска** информации о коллегах
- **Автоматизация актуализации** данных о сотрудниках

### Основные возможности

- **Интерактивная организационная карта** - древовидная структура компании с возможностью масштабирования и навигации
- **Поиск сотрудников** - быстрый поиск по ФИО, должности или отделу
- **Профили сотрудников** - детальная информация с возможностью редактирования
- **Административная панель** - управление данными и модерация для HR и администраторов

## 🛠 Технологии

- **Frontend**: React 18 + TypeScript
- **Стили**: UnoCSS с кастомной темой
- **UI Components**: PrimeReact
- **State Management**: Redux Toolkit
- **Роутинг**: React Router DOM
- **Сборка**: Vite

## 🧱 Архитектура

### Слои и провайдеры
- **AppProviders** объединяет PrimeReactProvider, Redux, AuthContext и BrowserRouter. Этот слой повторно используется и в `main.tsx`, и в тестовой утилите, поэтому конфигурация окружения везде идентична, а тесты автоматически получают все контексты.
- **React Router**: публичные маршруты (например, `/login`) описаны отдельно, защищённые находятся под `ProtectedRoute`, что упрощает добавление новых экранов без дублирования проверок.

### Состояние и данные
- **Redux Toolkit** отвечает только за взаимосвязанное состояние карты (зум, панорама). Это предотвращает лишние перерендеры и делает обработку жестов производительной.
- **Контексты и хук useAuth** управляют авторизацией и токеном. Через `AuthProvider` происходит bootstrap из localStorage и переключение между реальным API и мок-данными.
- **Сервисы (`services/*.ts`)** инкапсулируют работу с API и переключение `USE_MOCK_DATA`, поэтому компоненты не знают, откуда приходят данные.

### UI и стили
- **PrimeReact** даёт готовые таблицы, дропдауны и форму логина. Настройка тем делается глобально внутри AppProviders, что избавляет от повторяющихся провайдеров.
- **UnoCSS** используется для утилитарных классов и фирменной палитры; уникальные элементы (например, AdminButton) имеют локальные CSS-модули.

### Авторизация и права
- **AuthProvider + AuthContextInstance** централизуют хранение токена и предоставляют `getToken` для `fetchWithAuth`.
- **PermissionGuard/usePermissions** читают роль из локального хранилища и гарантируют, что административные разделы доступны только HR/админам.

### Тестирование
- **Vitest + Testing Library**: `src/test/test-utils.tsx` оборачивает компоненты теми же провайдерами, что и продакшен, при этом в тестовом окружении AuthProvider подменяется на лёгкий мок.
- **Покрытие 100%** контролируется обязательным запуском `npx vitest run --coverage` перед пушем.
