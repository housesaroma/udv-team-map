export enum UserRole {
  EMPLOYEE = "employee", // Обычный сотрудник
  HR = "hr", // HR специалист
  ADMIN = "admin", // Администратор
  SUPER_ADMIN = "super_admin", // Суперадминистратор (если нужно)
}
